{
  "name": "deep3d-model",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix Deep3D/front-end",
    "frontend": "npm run dev --prefix Deep3D/front-end",
    "backend": "./.venv/bin/python -m uvicorn backend.app:app --reload --host 0.0.0.0 --port 8000"
  }
}
